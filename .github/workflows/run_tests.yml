name: run_tests

on: workflow_call


jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run tests
        run: echo "ran tests"
  
  another_tests:
    runs-on: ubuntu-latest
    needs: run_tests
    if: false
    steps:
      - name: Run another after tests
        run: echo "ran another tests"

